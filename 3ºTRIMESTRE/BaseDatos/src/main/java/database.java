import java.sql.Connection;

public class database {
    
    import java.sql.*;
    public class DBConnector{
        private static Connection;
        public Connection getConnection(){
            Connection connection;
            return connection;
        }
    }
    //selecto from usuarios,id.nombrenu
    //sju rrtuy los prt juhy s or    juholllo
    //juer hy bc oir ki os lor  ,
     // juer ki rty
    //resulset a query
    //numero de despidos en la prueba de result,nufrf ru jnf uadmu r ki re

public class database {
 
    import java.sql.*;import java.sql.*;



    public class meter una coslr juher
    public class DBConnector{
        private static Connection;
        public Connection getConnection(){
            Connection connection;
            return connection;
        }
    }
}
//hacer un sistema que permita el login de los usuarios.para ello
//el sistema pedira el correo y pass del usuario que quiere acceder
//al sistema. EN caso de hacer login correct,
//aparecera el mensaje de bienvenido a la app
//en caso de no hacer login,aparecera el menssaje de
//fallo en la seguridad

    public int borrarUsuario(int venta) {
        int despidos = 0;
        String query = String.format("DELETE FROM %s WHERE %s<?"
                , //TAB_USER, SchemaDB.COL_AMOUNT);
        try {
            //ps = connection.prepareStatement(query);
            //ps.setInt(1,venta);
            despidos = ps.executeUpdate();

        } catch.where) {
            System.out.println("Fallo de ese SQL");
        }

        return despidos;
